export interface BssItem {
  bsIdx: string;
  bssIdx: string;
  bssName: string;
}

export interface BsItem {
  bsName: string;
  bssList: BssItem[];
}

export interface SelectedItem {
  bsIdx: string;
  bssIdx: string;
}
